import { formatMoney, summarizeExpenses } from '../domain/expenses';
import { healthScore } from '../domain/healthScore';
import { serviceTypeLabel } from '../domain/serviceTypes';
import type { FuelEntry, Reminder, ServiceRecord, Vehicle } from '../domain/types';
export function escapeHtml(s:string):string{return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
const row=(label:string,value:string)=>`<tr><td class="label">${escapeHtml(label)}</td><td>${escapeHtml(value)}</td></tr>`;
const miles=(n:number)=>`${n.toLocaleString('en-US')} mi`;
export type ReportInput={vehicle:Vehicle;records:ServiceRecord[];fuelEntries?:FuelEntry[];reminders:Reminder[];today:string};
export function buildVehicleReportHtml({vehicle,records,fuelEntries=[],reminders,today}:ReportInput):string{const health=healthScore({vehicle,records,reminders,today});const expenses=summarizeExpenses(records,today,fuelEntries);const services=[...records].sort((a,b)=>b.date.localeCompare(a.date)).map(r=>`<tr><td>${escapeHtml(r.date)}</td><td>${escapeHtml(serviceTypeLabel(r.serviceType))}</td><td>${r.mileage!=null?escapeHtml(miles(r.mileage)):''}</td><td>${escapeHtml(r.shopName??'')}</td><td class="num">${r.cost!=null?escapeHtml(formatMoney(r.cost)):''}</td></tr>${r.notes?`<tr class="notes"><td></td><td colspan="4">${escapeHtml(r.notes)}</td></tr>`:''}`).join('');const fuel=[...fuelEntries].sort((a,b)=>b.date.localeCompare(a.date)).map(f=>`<tr><td>${escapeHtml(f.date)}</td><td>${escapeHtml(miles(f.mileage))}</td><td>${f.gallons.toFixed(2)} gal</td><td>${escapeHtml(f.station??'')}</td><td class="num">${escapeHtml(formatMoney(f.totalCost))}</td></tr>`).join('');const vehicleName=`${vehicle.year} ${vehicle.make} ${vehicle.model}`;return `<!DOCTYPE html><html><head><meta charset="utf-8"/><style>@page{margin:36px}body{font-family:-apple-system,'Segoe UI',Roboto,Helvetica,Arial,sans-serif;color:#1c2128;font-size:12px}h1{font-size:22px;margin:0}h2{font-size:13px;text-transform:uppercase;letter-spacing:1px;color:#6b7280;margin:28px 0 8px}.meta{color:#6b7280;margin-top:4px}.brand{display:flex;justify-content:space-between;border-bottom:3px solid #F5A524;padding-bottom:12px}.app{font-weight:700;color:#b97a0a}.grid{display:flex;gap:12px;margin-top:12px}.tile{flex:1;border:1px solid #e2e5ea;border-radius:8px;padding:10px 12px}.k{color:#6b7280;font-size:10px;text-transform:uppercase}.v{font-size:16px;font-weight:700;margin-top:2px}table{width:100%;border-collapse:collapse}td,th{text-align:left;padding:6px 8px;border-bottom:1px solid #eceff3;vertical-align:top}th{font-size:10px;text-transform:uppercase;color:#6b7280}td.label{color:#6b7280;width:35%}.num{text-align:right}.notes td{color:#6b7280;font-size:11px}.footer{margin-top:32px;color:#9aa1ab;font-size:10px}</style></head><body><div class="brand"><div><h1>${escapeHtml(vehicleName)}</h1><div class="meta">Owner-maintained vehicle history${vehicle.trim?` · ${escapeHtml(vehicle.trim)}`:''}</div></div><div class="app">GLOVEBOX</div></div><div class="grid"><div class="tile"><div class="k">Odometer</div><div class="v">${escapeHtml(miles(vehicle.mileage))}</div></div><div class="tile"><div class="k">Health score</div><div class="v">${health.score} · ${escapeHtml(health.label)}</div></div><div class="tile"><div class="k">Service + fuel entries</div><div class="v">${records.length+fuelEntries.length}</div></div><div class="tile"><div class="k">Lifetime spend</div><div class="v">${escapeHtml(formatMoney(expenses.lifetimeTotal))}</div></div></div><h2>Vehicle details</h2><table>${row('VIN',vehicle.vin??'—')}${row('License plate',vehicle.licensePlate??'—')}${row('Purchased',vehicle.purchaseDate??'—')}${row('Purchase price',vehicle.purchasePrice!=null?formatMoney(vehicle.purchasePrice):'—')}</table><h2>Cost of ownership</h2><table>${row('This year',formatMoney(expenses.yearTotal))}${row('Maintenance',formatMoney(expenses.maintenanceTotal))}${row('Repairs',formatMoney(expenses.repairTotal))}${row('Fuel',formatMoney(expenses.fuelTotal))}${row('Registration & admin',formatMoney(expenses.adminTotal))}${row('Cost per mile',expenses.costPerMile!=null?`$${expenses.costPerMile.toFixed(2)}`:'—')}</table><h2>Service history (${records.length})</h2>${records.length?`<table><tr><th>Date</th><th>Service</th><th>Mileage</th><th>Shop</th><th class="num">Cost</th></tr>${services}</table>`:'<p class="meta">No service records.</p>'}<h2>Fuel history (${fuelEntries.length})</h2>${fuelEntries.length?`<table><tr><th>Date</th><th>Mileage</th><th>Gallons</th><th>Station</th><th class="num">Cost</th></tr>${fuel}</table>`:'<p class="meta">No fuel entries.</p>'}<div class="footer">Generated by Glovebox on ${escapeHtml(today)}. Health Score is a maintenance-record heuristic based on owner-entered records, not a mechanical inspection or diagnosis.</div></body></html>`;}
